{
  "name": "seat-allocation-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon app.js",
    "build": "cd Client && npm run build",
    "heroku-postbuild" : "npm run install-client && num install -g @angular/cli && npm run build",
    "install-client": "cd Client && npm install"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mongoose": "^6.3.0",
    "morgan": "~1.9.1"
  },
  "engines": {
    "node": "16.x"
  }
}
